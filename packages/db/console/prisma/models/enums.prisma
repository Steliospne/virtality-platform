// Shared Enums

enum Language {
  Greek
  English
}

enum Platform {
  web
  vr
}

enum ReusableProgramKind {
  CLINICIAN_OWNED
  STARTER_TEMPLATE
}

enum PatientSessionStatus {
  ACTIVE
  COMPLETED
  INTERRUPTED
}

enum PendingPasswordChangeKind {
  SETUP
  CHANGE
}

enum PendingPasswordChangeStatus {
  PENDING
  APPROVED
  CANCELLED
  SUPERSEDED
}

enum DevicePairingStatus {
  PENDING
  COMPLETED
  CANCELLED
  SUPERSEDED
  EXPIRED
}

enum TrialRedeemCodeStatus {
  unused
  redeemed
  already_entitled
}

enum TrialRedeemCodeMode {
  permanent_free
  timed_trial
}

enum PromotionCodeDeliveryStatus {
  open
}

enum PendingPromotionCodeStatus {
  open
  applied
  canceled
  failed
  expired
}
